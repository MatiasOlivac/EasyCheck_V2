import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'EasyCheck_V2',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
